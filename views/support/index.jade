extends ../layout

block content
  .well
    h1 Support Index
    p This is a normally routed view that could be made interactive.
    p It could for instance collate articles and maybe have a form to submitting support requests.

  ul
    each page in pages
      li
        | [ #{page.date} ] 
        a(href='support/pages/#{page.filename}')=page.title
